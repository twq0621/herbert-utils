package net.snake.gamemodel.heroext.lianti.response;

import net.snake.netio.ServerResponse;

public class CharacterLianTiInfoResponse53002 extends ServerResponse {
	//
	// public CharacterLianTiInfoResponse53002(Hero c) {
	// setMsgCode(53002);
	// // "角色ID（int），
	// // 当前炼体级别（byte,1到8），加生命(int), 加内力(int), 加体力(int), 加攻(int), 加攻速(int), 加移速(int),加反弹伤害（int），加伤害减免(int),加暗器几率(int) "
	// // try {
	// CharacterLianTiController liantic=c.getLiantiController();
	// writeInt(c.getId());
	// writeByte(liantic.getLiantiJingjieId());
	// writeInt(liantic.getHp());
	// writeInt(liantic.getMp());
	// writeInt(liantic.getSp());
	// writeInt(liantic.getAtk());
	// writeInt(liantic.getDef());
	// writeInt(liantic.getAtkSpeed());
	//
	// writeInt(liantic.getMoveSpeed());
	// writeInt(liantic.getFjsh());
	// writeInt(liantic.getShjm());
	// writeInt(liantic.getAqJv());
	// // } catch (IOException e) {
	// // logger.error(e.getMessage(),e);
	// // }
	//
	// }

}
